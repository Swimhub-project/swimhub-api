/*
  "update entry" controller function

  Updates an existing entry from the database by the entryId property, using data from the body params. 
*/

//import packages
import { Request, Response } from 'express';

export const updateEntry = (req: Request, res: Response) => {};
