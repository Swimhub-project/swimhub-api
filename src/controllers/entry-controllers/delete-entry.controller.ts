/*
  "delete entry" controller function

  Deletes an existing entry from the database by the entryId property. 
*/

//import packages
import { Request, Response } from 'express';

export const deleteEntry = (req: Request, res: Response) => {};
