/*
  "get single entry" controller function

  Gets a single entry by the entryId property. 
*/

//import packages
import { Request, Response } from 'express';

export const getEntryById = (req: Request, res: Response) => {};
